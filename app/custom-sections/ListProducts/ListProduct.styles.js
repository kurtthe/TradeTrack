import {StyleSheet} from 'react-native'

export const makeStyles = ()=> 
StyleSheet.create({
  contentProducts: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    flexWrap: 'wrap',
  }
})